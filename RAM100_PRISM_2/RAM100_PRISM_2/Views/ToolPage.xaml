<UserControl x:Class="RAM100_PRISM_2.Views.ToolPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAM100_PRISM_2.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1080">
    <UserControl.Resources>
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Padding="{TemplateBinding Padding}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="加料助手计算器" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- 总重输入 -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="总重量:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
            <TextBox Text="{Binding TotalWeight, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" 
                     Width="100" VerticalAlignment="Center"/>
            <TextBlock Text="kg" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- 验证信息 - 只显示错误信息 -->
        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="LightYellow" 
                BorderBrush="Goldenrod" BorderThickness="1" 
                Margin="0,0,0,10" Padding="10,5"
                Visibility="{Binding ValidationMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding ValidationMessage, Mode=OneWay}" 
                       Foreground="DarkRed" FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="3" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding Ingredients}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="成分名称" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="占比 (%)" 
                                   Binding="{Binding Percentage, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="重量 (kg)" 
                                   Binding="{Binding Weight, StringFormat=N2, Mode=OneWay}" 
                                   IsReadOnly="True" Width="120"/>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="删除" 
                                   Command="{Binding DataContext.RemoveIngredientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                   CommandParameter="{Binding}"
                                   Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 按钮区域 - 只保留添加和清空按钮 -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,0,0,15">
            <Button Content="添加成分" Command="{Binding AddIngredientCommand}" 
                    Padding="15,5" Margin="0,0,10,0"/>
            <Button Content="清空重置" Command="{Binding ClearCommand}" 
                    Padding="15,5" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- 统计信息 --><!--
        <Border Grid.Row="5" Grid.ColumnSpan="2" Background="LightBlue" 
                BorderBrush="SteelBlue" BorderThickness="1" Padding="10" Margin="0,10,0,0">
            <TextBlock>
                <Run Text="成分数量:" FontWeight="Bold"/>
                <Run Text="{Binding IngredientCount, Mode=OneWay}"/>
                <Run Text=" | "/>
                <Run Text="总占比:" FontWeight="Bold"/>
                <Run Text="{Binding TotalPercentageValue, StringFormat=N2, Mode=OneWay}"/>
                <Run Text="%"/>
            </TextBlock>
        </Border>-->
    </Grid>
</UserControl>