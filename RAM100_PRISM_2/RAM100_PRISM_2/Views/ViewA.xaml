<UserControl x:Class="RAM100_PRISM_2.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAM100_PRISM_2.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:viewmodels="clr-namespace:RAM100_PRISM_2.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ViewAViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="GhostWhite">
        <TextBlock FontSize="30"
                   FontFamily="Segoe Print"
                   
                   Text="声共振混合机" 
                   VerticalAlignment="Top" 
                   HorizontalAlignment="Left" 
                   Margin="15 5"></TextBlock>
        <Grid>
            <ItemsControl ItemsSource="{Binding PrimaryList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Name="Grid" Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <StackPanel VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding Value}" HorizontalAlignment="Center" Foreground="Black"/>
                                <!--<TextBlock Text="{Binding XLable}" HorizontalAlignment="Center" Foreground="Black"/>-->
                                <Border Width="10" Height="{Binding Value}" Background="Orange"
                                        VerticalAlignment="Bottom" CornerRadius="5, 5, 0, 0"/>
                            </StackPanel>
                            <Border BorderBrush="#DDD" BorderThickness="0, 1, 0, 0" Grid.Row="1"/>
                            <TextBlock Text="{Binding Label}" Grid.Row="1" VerticalAlignment="Center"  Foreground="Black"
                                       HorizontalAlignment="Center"/>
                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Grid" Property="Background" Value="#EEE"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate> 
            </ItemsControl>
            
        </Grid>

        <TextBlock FontSize="80" Text="{Binding Title}" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        
    </Grid>
</UserControl>
