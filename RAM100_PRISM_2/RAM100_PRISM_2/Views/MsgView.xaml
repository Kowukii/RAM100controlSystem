<UserControl x:Class="RAM100_PRISM_2.Views.MsgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAM100_PRISM_2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Beige">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- 标题文本 -->
        <TextBlock 
                   Grid.Row="0"
                   Text="编辑窗口" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Top" 
                   Margin="10,10,0,0" 
                   FontSize="16" FontWeight="Bold"/>

        <!-- 输入框（可根据需求改为TextBox等） -->
        
                

                <!-- 这里示例放一个空的 TextBlock 占位，实际可替换为输入控件 -->
        <TextBox  Grid.Row="1" Text="{Binding Title}" 
                  VerticalAlignment="Center" 
                  HorizontalAlignment="Center"
                  Width="249"
                  Opacity="0.5" />

         

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom" 
                    Margin="0,0,10,10">
            <Button Content="确定" Width="80" Margin="0,0,10,0"  Command="{Binding SaveCommand}"
                    />
            <Button Content="取消" Width="80" Command="{Binding CancelCommand}"
                    />
        </StackPanel>

    </Grid>
</UserControl>
