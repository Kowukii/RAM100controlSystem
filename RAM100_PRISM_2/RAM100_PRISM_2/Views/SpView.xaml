<UserControl x:Class="RAM100_PRISM_2.Views.SpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAM100_PRISM_2.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:b="clr-namespace:RAM100_PRISM_2.Base"
            
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WpfPlot Name="wpfPlot"/>
        <WpfPlot Grid.Row="1" b:ScottPlotExtension.Values="{Binding}"/>
    </Grid>-->
    <Grid Margin="10" Background="AliceBlue">
        <!-- 顶部状态栏 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 时间和设备状态区域 -->
        <Grid Grid.Row="0" Background="#2D3748" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label x:Name="TimeLabel" Grid.Column="0" Foreground="White" Margin="10,5" HorizontalAlignment="Left" FontSize="14"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,5">
                <Label Content="设备状态:" Foreground="White" Margin="0,0,5,0"/>
                <Label x:Name="DeviceStatusLabel" Foreground="#48BB78" FontSize="14"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,5">
                <Label Content="连接状态:" Foreground="White" Margin="0,0,5,0"/>
                <Label x:Name="ConnectionStatusLabel" Foreground="#48BB78" FontSize="14"/>
            </StackPanel>

            <Button Grid.Column="3" Content="连接设备" x:Name="ConnectButton" Margin="10,5" Width="100"/>
        </Grid>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：加速度传感器数据显示区域 -->
            <GroupBox Header="加速度传感器数据" Grid.Column="0">
                <TabControl Margin="5">
                    <TabItem Header="实时数据">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 图表显示 -->
                            <!--<charting:Chart Grid.Row="0" Margin="5" Title="加速度实时曲线">
                             <charting:LineSeries x:Name="XAccelerationSeries" Title="X轴加速度" 
                                                  IndependentValueBinding="{Binding Time}" 
                                                  DependentValueBinding="{Binding Value}"/>
                             <charting:LineSeries x:Name="YAccelerationSeries" Title="Y轴加速度" 
                                                  IndependentValueBinding="{Binding Time}" 
                                                  DependentValueBinding="{Binding Value}"/>
                             <charting:LineSeries x:Name="ZAccelerationSeries" Title="Z轴加速度" 
                                                  IndependentValueBinding="{Binding Time}" 
                                                  DependentValueBinding="{Binding Value}"/>
                         </charting:Chart>-->
                            <WpfPlot Name="wpfPlot"/>
                            <WpfPlot b:ScottPlotExtension.Values="{Binding}"/>

                            <!-- 数值显示 -->
                            <Grid Grid.Row="1" Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Label Content="X轴: " FontWeight="Bold"/>
                                    <Label x:Name="XValueLabel" Content="0.00" Foreground="#E53E3E" FontSize="16"/>
                                    <Label Content=" m/s²"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Label Content="Y轴: " FontWeight="Bold"/>
                                    <Label x:Name="YValueLabel" Content="0.00" Foreground="#38A169" FontSize="16"/>
                                    <Label Content=" m/s²"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Label Content="Z轴: " FontWeight="Bold"/>
                                    <Label x:Name="ZValueLabel" Content="0.00" Foreground="#3182CE" FontSize="16"/>
                                    <Label Content=" m/s²"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="数据历史">
                        <Grid>
                            <DataGrid x:Name="HistoryDataGrid" Margin="5" AutoGenerateColumns="True" IsReadOnly="True"/>
                        </Grid>
                    </TabItem>
                </TabControl>
            </GroupBox>

            <!-- 右侧：控制区域 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 频率控制区域 -->
                <GroupBox Header="频率控制" Grid.Row="0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="采样频率:" Width="100" VerticalAlignment="Center"/>
                            <TextBox x:Name="FrequencyTextBox" Width="100" Text="100" HorizontalAlignment="Left"/>
                            <Label Content=" Hz" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Slider x:Name="FrequencySlider" Minimum="10" Maximum="500" Value="100" 
                             TickFrequency="10" IsSnapToTickEnabled="True"/>

                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="当前频率:" Width="100" VerticalAlignment="Center"/>
                            <Label x:Name="CurrentFrequencyLabel" Content="100 Hz" Foreground="#2C6ECB" FontSize="14"/>
                        </StackPanel>

                        <Button Content="应用频率设置" x:Name="ApplyFrequencyButton" HorizontalAlignment="Center"/>
                    </StackPanel>
                </GroupBox>

                <!-- 强度控制区域 -->
                <GroupBox Header="强度控制" Grid.Row="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="传感器灵敏度:" Width="120" VerticalAlignment="Center"/>
                            <TextBox x:Name="SensitivityTextBox" Width="100" Text="50" HorizontalAlignment="Left"/>
                            <Label Content=" %" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Slider x:Name="SensitivitySlider" Minimum="10" Maximum="100" Value="50" 
                             TickFrequency="5" IsSnapToTickEnabled="True"/>

                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="当前灵敏度:" Width="120" VerticalAlignment="Center"/>
                            <Label x:Name="CurrentSensitivityLabel" Content="50 %" Foreground="#2C6ECB" FontSize="14"/>
                        </StackPanel>

                        <Separator Margin="5" Background="#CBD5E0"/>

                        <StackPanel Margin="5">
                            <Label Content="操作模式:" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <RadioButton x:Name="NormalModeRadio" Content="正常模式" IsChecked="True" Margin="5"/>
                                <RadioButton x:Name="HighPrecisionModeRadio" Content="高精度模式" Margin="5"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Content="应用强度设置" x:Name="ApplyIntensityButton" HorizontalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button Content="开始采集" x:Name="StartButton" Background="#38A169"/>
                            <Button Content="停止采集" x:Name="StopButton" Background="#E53E3E"/>
                            <Button Content="重置" x:Name="ResetButton" Background="#ED8936"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>

</UserControl>
